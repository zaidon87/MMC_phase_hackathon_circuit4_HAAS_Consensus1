# Original Simulink Model

Place the original binary model here:

```text
MMC_phase_hackathon_circuit4_HAAS_Consensus1.slx
```

In the prepared local ZIP, this file exists under this folder. The current GitHub connector session uploaded the reviewable text/code structure. If the `.slx` file is missing on GitHub, upload it manually from your computer or push the prepared ZIP with Git.

Recommended command from the repository root:

```bash
git add MMC_models/original/MMC_phase_hackathon_circuit4_HAAS_Consensus1.slx
git commit -m "Add original Simulink MMC model"
git push
```
